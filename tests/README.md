## Testing

Testing and validation for this project were performed through dedicated
Jupyter notebooks.

The notebooks cover component-level testing, integration testing,
pipeline validation, benchmarking, and failure-case evaluation.

The notebooks are used only for testing/validation; the core application
is implemented as standalone Python code and runs independently of Jupyter.
